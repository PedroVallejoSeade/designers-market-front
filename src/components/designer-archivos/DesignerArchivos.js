import React from 'react'
import DesignerNavBar from '../common/DesignerNavBar'
import DesignerArchivosContent from './DesignerArchivosContent'

const DesignerArchivos = () => {
    return (
        <div>
        <div className="designer-pages-container">
       <DesignerNavBar/>
       <DesignerArchivosContent/>
   </div>
   </div>
    )
}

export default DesignerArchivos
