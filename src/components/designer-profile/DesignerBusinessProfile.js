import React from 'react'
import DesignerNavBar from '../common/DesignerNavBar'
import DesignerProfileContent from './DesignerProfileContent'

const DesignerBusinessProfile = () => {
    return (
        <div className="business-pages-container">
            <DesignerNavBar/>
            <DesignerProfileContent/>
        </div>
    )
}

export default DesignerBusinessProfile