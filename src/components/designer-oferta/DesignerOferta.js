import React from 'react'
import DesignerNavBar from '../common/DesignerNavBar'
import DesignerOfertaContent from './DesignerOfertaContent'

const DesignerOferta = () => {
    return (
        <div>
             <div className="designer-pages-container">
            <DesignerNavBar/>
            <DesignerOfertaContent/>
        </div>
        </div>
    )
}

export default DesignerOferta
